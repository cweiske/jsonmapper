language: php
php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - hhvm

before_script:
  - pear install pear/PHP_CodeSniffer
  - phpenv rehash
script:
  - cd tests
  - phpunit --coverage-text .
  - cd ..
  - phpcs --standard=PEAR src/
